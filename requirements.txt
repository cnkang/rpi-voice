# Core libraries for API interactions
openai>=0.16.0 # Updated to reflect minimum versions if compatibility issues arise

# Environment configuration management
python-dotenv>=0.20.0

# Audio handling and playback
pydub>=0.25.1
sounddevice>=0.4.4

# Numerical operations and scientific computing
numpy>=1.22.2
scipy>=1.7.1

# Voice Activity Detection
webrtcvad>=2.0.10

# Python project packaging tools
setuptools>=65.5.1

# Asynchronous HTTP client with HTTP/2 support
httpx[http2]>=0.23.0

# Coroutine-based asynchronous I/O
asyncio>=3.4.3 # Included in Python's standard library; listed here for completeness

# Error handling and logging support in async environments
aiologger>=0.6.0 # Optional: For more advanced asynchronous logging capabilities

# For HTTP error handling in asynchronous calls
httpx-errors>=1.0 # Hypothetical package for better error management with httpx
